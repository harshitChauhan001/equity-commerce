{
  "name": "equity-commerce",
  "version": "1.0.0",
  "description": "Item Marketplace with personalized pricing, configurable offers, and race-condition-safe ordering",
  "private": true,
  "scripts": {
    "server": "cd server && npm start",
    "server:dev": "cd server && npm run dev",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server:dev\" \"npm run client\"",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "db:migrate": "cd server && npx sequelize-cli db:migrate",
    "db:seed": "cd server && npx sequelize-cli db:seed:all",
    "db:setup": "npm run db:migrate && npm run db:seed",
    "setup": "npm run install:all && npm run db:setup"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
